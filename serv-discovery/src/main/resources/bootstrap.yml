server:
  port: 8082

spring:
  application:
    name: eurekaserver

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/
    fetch-registry: false
    register-with-eureka: false
